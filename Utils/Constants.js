export const INPUT_MAX_LENGTHS = {
  USERNAME: 20,
  EMAIL: 50,
  PASSWORD: 20,
};

export const VALIDATION_PATTERNS = {
  USERNAME: /^[a-zA-Z0-9]*$/,
  EMAIL: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  PASSWORD_MIN_LENGTH: 6,
};
